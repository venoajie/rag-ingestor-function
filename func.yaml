
schema_version: 20180708
name: rag-ingestor
version: 3.0.0
runtime: docker
memory: 1024
timeout: 300
annotations:
  # This tells the API Gateway and Fn platform which path inside the
  # container the function is listening on.
  fn.oci.oracle.com/fn/invokeEndpoint: "/call"